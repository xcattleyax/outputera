<div class="main-bottom">
  <p>pick up! [ノート]</p>
  <% @notes.each do |note| %>
    <div class="note-selection">
      <p>タイトル: <%= note.title %></p>
      <p>カテゴリー: <%= note.category.name %></p>
    </div>
  <% end %>
</div>