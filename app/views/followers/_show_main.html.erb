<div>
  <h2>フォロー数</h2>
  <%= "#{@follow_number}人" %>
  <div class="follow-bottom">
    <% @followers.each do |follower| %>
      <%= link_to follower.user.name, post_path(follower.user_id)%>
    <% end %>
  </div>
</div>