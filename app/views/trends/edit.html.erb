カテゴリー
<%= form_with url: trend_path, method: :delete, local: true do |f| %>
  <%= f.collection_select(:category_id, Category.where(id: @trend_categories), :id, :name, {}, {class:"diary-category"}) %>
    <%= f.submit '決定', class: "button" %>
<% end %>