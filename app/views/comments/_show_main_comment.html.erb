<div class="show-comment-space">
  <h3>ノートコメント一覧</h3>
  <% @comments.each do |comment| %>
    <div>
      <%= comment.note.title %>
      <%= link_to "#{comment.user.name}さんから", note_note_comment_path(note_id: comment.note_id, id: comment.id)%>
    </div>
  <% end %>
</div>